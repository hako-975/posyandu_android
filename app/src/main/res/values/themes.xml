<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Posyandu" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/blue</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/white</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Posyandu.Material3.Light" parent="Theme.Material3.Light">
        <item name="colorPrimary">@color/blue</item> <!-- Change to your desired color -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/blue</item>
        <!-- Other theme attributes -->
    </style>

    <style name="Theme.Posyandu.Material3.Light.NoActionBar" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/blue</item> <!-- Change to your desired color -->
        <item name="android:statusBarColor" tools:targetApi="21">@color/blue</item>
        <!-- Other theme attributes -->
    </style>

    <style name="Theme.Material3.Light.Fullscreen" parent="Theme.Material3.Light">
        <item name="android:actionBarStyle">
            @style/Widget.Theme.Material3.Light.ActionBar.Fullscreen
        </item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowBackground">@null</item>
    </style>

    <style name="ThemeOverlay.Posyandu.FullscreenContainer" parent="">
        <item name="fullscreenBackgroundColor">@color/light_blue_600</item>
        <item name="fullscreenTextColor">@color/light_blue_A200</item>
    </style>
</resources>